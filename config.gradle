/**
 * 全局统一配置
 */
ext {
    /**
     * module开关统一声明在此处
     * true：module作为application，可单独打包为apk
     * false：module作为library，可作为宿主application的组件
     */

    /**
     * 版本统一管理
     */
    applicationId = "com.xh.robot"
    versionCode = 1
    versionName = "1.0.0"

    compileSdkVersion = 28
    minSdkVersion = 21
    targetSdkVersion = 28

    kotlin_version = "1.3.50"
    arouterApiVersion = "1.2.2"

    permissionsApiVersion = "4.5.0"
}